import React, { useState, useEffect } from 'react';
import { questions } from '../../data';

function Game() {
  const [score, setScore] = useState(0);
  return (
    <div id="game">
      <div>Game</div>
    </div>
  );
}

export default Game;
